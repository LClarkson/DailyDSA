# Create Node class
class Node:
    def __init__(self, data):
        self.data = data
        self.left = None
        self.right = None

class BinarySearchTree:
    def __init__(self):
        self.root = None

    def insert(self, data):
        # create new node
        # if root is empty, root = new node
        # create a curr variable to point at nodes, set it = root to start at top of tree
        # while loop
            # if new node = temp, return False
            # compare new node to root, if < go left, else go right
            # if insertion point is None, insert node, else move to next node
        return

    def contains(self, data):
        # if else
        return
    

newTree = BinarySearchTree()
print(newTree)